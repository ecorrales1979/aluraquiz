export interface Theme {
  colors: {
    primary: string
    secondary: string
    mainBg: string
    contrastText: string
    shadowText: string
    wrong: string
    success: string
  },
  borderRadius: string
}
